spring:
  application:
    name: auth-service
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      uri: http://localhost:7888
      name: auth-service
      fail-fast: true
  profiles:
    active: conf